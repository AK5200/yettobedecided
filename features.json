[
  {
    "id": 1,
      "category": "database",
      "description": "Create tags table",
    "steps": [
        "Step 1: Create file supabase/migrations/005_tags.sql",
        "Step 2: Add this SQL:",
        "  CREATE TABLE IF NOT EXISTS tags (",
        "    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),",
        "    org_id UUID REFERENCES organizations(id) ON DELETE CASCADE,",
        "    name TEXT NOT NULL,",
        "    color TEXT NOT NULL DEFAULT '#6B7280',",
        "    created_at TIMESTAMPTZ DEFAULT NOW(),",
        "    UNIQUE(org_id, name)",
        "  );",
        "Step 3: Save the file",
        "Step 4: Log: User must run this SQL in Supabase SQL Editor",
        "Step 5: Run: git add -A && git commit -m 'feat: add tags table'"
      ],
      "passes": true
  },
  {
    "id": 2,
      "category": "database",
      "description": "Create post_tags junction table",
    "steps": [
        "Step 1: Create file supabase/migrations/006_post_tags.sql",
        "Step 2: Add this SQL:",
        "  CREATE TABLE IF NOT EXISTS post_tags (",
        "    post_id UUID REFERENCES posts(id) ON DELETE CASCADE,",
        "    tag_id UUID REFERENCES tags(id) ON DELETE CASCADE,",
        "    created_at TIMESTAMPTZ DEFAULT NOW(),",
        "    PRIMARY KEY (post_id, tag_id)",
        "  );",
        "Step 3: Save the file",
        "Step 4: Log: User must run this SQL in Supabase SQL Editor",
        "Step 5: Run: git add -A && git commit -m 'feat: add post_tags table'"
    ],
    "passes": false
  },
  {
    "id": 3,
      "category": "database",
      "description": "Create invitations table",
    "steps": [
        "Step 1: Create file supabase/migrations/007_invitations.sql",
        "Step 2: Add this SQL:",
        "  CREATE TABLE IF NOT EXISTS invitations (",
        "    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),",
        "    org_id UUID REFERENCES organizations(id) ON DELETE CASCADE,",
        "    email TEXT NOT NULL,",
        "    role TEXT NOT NULL DEFAULT 'member',",
        "    invited_by UUID,",
        "    token TEXT NOT NULL UNIQUE,",
        "    expires_at TIMESTAMPTZ NOT NULL,",
        "    accepted_at TIMESTAMPTZ,",
        "    created_at TIMESTAMPTZ DEFAULT NOW()",
        "  );",
        "Step 3: Save the file",
        "Step 4: Log: User must run this SQL in Supabase SQL Editor",
        "Step 5: Run: git add -A && git commit -m 'feat: add invitations table'"
    ],
    "passes": false
  },
  {
    "id": 4,
      "category": "database",
      "description": "Create notification_preferences table",
    "steps": [
        "Step 1: Create file supabase/migrations/008_notification_preferences.sql",
        "Step 2: Add this SQL:",
        "  CREATE TABLE IF NOT EXISTS notification_preferences (",
        "    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),",
        "    user_id UUID NOT NULL,",
        "    email_on_status_change BOOLEAN DEFAULT true,",
        "    email_on_comment BOOLEAN DEFAULT true,",
        "    email_on_new_feedback BOOLEAN DEFAULT true,",
        "    email_digest BOOLEAN DEFAULT false,",
        "    created_at TIMESTAMPTZ DEFAULT NOW(),",
        "    updated_at TIMESTAMPTZ DEFAULT NOW(),",
        "    UNIQUE(user_id)",
        "  );",
        "Step 3: Save the file",
        "Step 4: Log: User must run this SQL in Supabase SQL Editor",
        "Step 5: Run: git add -A && git commit -m 'feat: add notification_preferences table'"
    ],
    "passes": false
  },
  {
    "id": 5,
      "category": "database",
      "description": "Create webhooks table",
    "steps": [
        "Step 1: Create file supabase/migrations/009_webhooks.sql",
        "Step 2: Add this SQL:",
        "  CREATE TABLE IF NOT EXISTS webhooks (",
        "    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),",
        "    org_id UUID REFERENCES organizations(id) ON DELETE CASCADE,",
        "    name TEXT NOT NULL,",
        "    url TEXT NOT NULL,",
        "    secret TEXT,",
        "    events TEXT[] NOT NULL DEFAULT '{}',",
        "    is_active BOOLEAN DEFAULT true,",
        "    created_at TIMESTAMPTZ DEFAULT NOW(),",
        "    updated_at TIMESTAMPTZ DEFAULT NOW()",
        "  );",
        "Step 3: Save the file",
        "Step 4: Log: User must run this SQL in Supabase SQL Editor",
        "Step 5: Run: git add -A && git commit -m 'feat: add webhooks table'"
    ],
    "passes": false
  },
  {
    "id": 6,
      "category": "database",
      "description": "Create api_keys table",
    "steps": [
        "Step 1: Create file supabase/migrations/010_api_keys.sql",
        "Step 2: Add this SQL:",
        "  CREATE TABLE IF NOT EXISTS api_keys (",
        "    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),",
        "    org_id UUID REFERENCES organizations(id) ON DELETE CASCADE,",
        "    name TEXT NOT NULL,",
        "    key_hash TEXT NOT NULL,",
        "    key_prefix TEXT NOT NULL,",
        "    last_used_at TIMESTAMPTZ,",
        "    expires_at TIMESTAMPTZ,",
        "    created_at TIMESTAMPTZ DEFAULT NOW()",
        "  );",
        "Step 3: Save the file",
        "Step 4: Log: User must run this SQL in Supabase SQL Editor",
        "Step 5: Run: git add -A && git commit -m 'feat: add api_keys table'"
    ],
    "passes": false
  },
  {
    "id": 7,
      "category": "database",
      "description": "Create changelog_subscribers table",
    "steps": [
        "Step 1: Create file supabase/migrations/011_changelog_subscribers.sql",
        "Step 2: Add this SQL:",
        "  CREATE TABLE IF NOT EXISTS changelog_subscribers (",
        "    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),",
        "    org_id UUID REFERENCES organizations(id) ON DELETE CASCADE,",
        "    email TEXT NOT NULL,",
        "    is_verified BOOLEAN DEFAULT false,",
        "    unsubscribed_at TIMESTAMPTZ,",
        "    created_at TIMESTAMPTZ DEFAULT NOW(),",
        "    UNIQUE(org_id, email)",
        "  );",
        "Step 3: Save the file",
        "Step 4: Log: User must run this SQL in Supabase SQL Editor",
        "Step 5: Run: git add -A && git commit -m 'feat: add changelog_subscribers table'"
    ],
    "passes": false
  },
  {
    "id": 8,
      "category": "database",
      "description": "Create integrations table for Slack/Discord",
    "steps": [
        "Step 1: Create file supabase/migrations/012_integrations.sql",
        "Step 2: Add this SQL:",
        "  CREATE TABLE IF NOT EXISTS integrations (",
        "    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),",
        "    org_id UUID REFERENCES organizations(id) ON DELETE CASCADE,",
        "    type TEXT NOT NULL,",
        "    webhook_url TEXT,",
        "    channel_name TEXT,",
        "    notify_on_new_feedback BOOLEAN DEFAULT true,",
        "    notify_on_status_change BOOLEAN DEFAULT true,",
        "    notify_on_new_comment BOOLEAN DEFAULT false,",
        "    is_active BOOLEAN DEFAULT true,",
        "    created_at TIMESTAMPTZ DEFAULT NOW(),",
        "    updated_at TIMESTAMPTZ DEFAULT NOW(),",
        "    UNIQUE(org_id, type)",
        "  );",
        "Step 3: Save the file",
        "Step 4: Log: User must run this SQL in Supabase SQL Editor",
        "Step 5: Run: git add -A && git commit -m 'feat: add integrations table'"
    ],
    "passes": false
  },
  {
    "id": 9,
      "category": "database",
      "description": "Add columns to organizations table",
    "steps": [
        "Step 1: Create file supabase/migrations/013_org_columns.sql",
        "Step 2: Add this SQL:",
        "  ALTER TABLE organizations ADD COLUMN IF NOT EXISTS description TEXT;",
        "  ALTER TABLE organizations ADD COLUMN IF NOT EXISTS logo_url TEXT;",
        "  ALTER TABLE organizations ADD COLUMN IF NOT EXISTS website TEXT;",
        "Step 3: Save the file",
        "Step 4: Log: User must run this SQL in Supabase SQL Editor",
        "Step 5: Run: git add -A && git commit -m 'feat: add org description and logo columns'"
    ],
    "passes": false
  },
  {
    "id": 10,
      "category": "database",
      "description": "Add is_archived column to boards table",
    "steps": [
        "Step 1: Create file supabase/migrations/014_board_archived.sql",
        "Step 2: Add this SQL:",
        "  ALTER TABLE boards ADD COLUMN IF NOT EXISTS is_archived BOOLEAN DEFAULT false;",
        "Step 3: Save the file",
        "Step 4: Log: User must run this SQL in Supabase SQL Editor",
        "Step 5: Run: git add -A && git commit -m 'feat: add is_archived to boards'"
    ],
    "passes": false
  },
  {
    "id": 11,
      "category": "types",
      "description": "Add Tag type to database.ts",
    "steps": [
        "Step 1: Open file lib/types/database.ts",
        "Step 2: Add at the end of the file:",
        "  export interface Tag {",
        "    id: string",
        "    org_id: string",
        "    name: string",
        "    color: string",
        "    created_at: string",
        "  }",
        "Step 3: Save the file",
        "Step 4: Run: npm run build",
        "Step 5: Run: git add -A && git commit -m 'feat: add Tag type'"
    ],
    "passes": false
  },
  {
    "id": 12,
      "category": "types",
      "description": "Add Invitation type to database.ts",
    "steps": [
        "Step 1: Open file lib/types/database.ts",
        "Step 2: Add at the end of the file:",
        "  export interface Invitation {",
        "    id: string",
        "    org_id: string",
        "    email: string",
        "    role: 'owner' | 'admin' | 'member'",
        "    invited_by: string",
        "    token: string",
        "    expires_at: string",
        "    accepted_at: string | null",
        "    created_at: string",
        "  }",
        "Step 3: Save the file",
        "Step 4: Run: npm run build",
        "Step 5: Run: git add -A && git commit -m 'feat: add Invitation type'"
    ],
    "passes": false
  },
  {
    "id": 13,
      "category": "types",
      "description": "Add NotificationPreferences type to database.ts",
    "steps": [
        "Step 1: Open file lib/types/database.ts",
        "Step 2: Add at the end of the file:",
        "  export interface NotificationPreferences {",
        "    id: string",
        "    user_id: string",
        "    email_on_status_change: boolean",
        "    email_on_comment: boolean",
        "    email_on_new_feedback: boolean",
        "    email_digest: boolean",
        "    created_at: string",
        "    updated_at: string",
        "  }",
        "Step 3: Save the file",
        "Step 4: Run: npm run build",
        "Step 5: Run: git add -A && git commit -m 'feat: add NotificationPreferences type'"
    ],
    "passes": false
  },
  {
    "id": 14,
      "category": "types",
      "description": "Add Webhook type to database.ts",
    "steps": [
        "Step 1: Open file lib/types/database.ts",
        "Step 2: Add at the end of the file:",
        "  export interface Webhook {",
        "    id: string",
        "    org_id: string",
        "    name: string",
        "    url: string",
        "    secret: string | null",
        "    events: string[]",
        "    is_active: boolean",
        "    created_at: string",
        "    updated_at: string",
        "  }",
        "Step 3: Save the file",
        "Step 4: Run: npm run build",
        "Step 5: Run: git add -A && git commit -m 'feat: add Webhook type'"
    ],
    "passes": false
  },
  {
    "id": 15,
      "category": "types",
      "description": "Add ApiKey type to database.ts",
    "steps": [
        "Step 1: Open file lib/types/database.ts",
        "Step 2: Add at the end of the file:",
        "  export interface ApiKey {",
        "    id: string",
        "    org_id: string",
        "    name: string",
        "    key_hash: string",
        "    key_prefix: string",
        "    last_used_at: string | null",
        "    expires_at: string | null",
        "    created_at: string",
        "  }",
        "Step 3: Save the file",
        "Step 4: Run: npm run build",
        "Step 5: Run: git add -A && git commit -m 'feat: add ApiKey type'"
    ],
    "passes": false
  },
  {
    "id": 16,
      "category": "types",
      "description": "Add Integration type to database.ts",
    "steps": [
        "Step 1: Open file lib/types/database.ts",
        "Step 2: Add at the end of the file:",
        "  export interface Integration {",
        "    id: string",
        "    org_id: string",
        "    type: 'slack' | 'discord'",
        "    webhook_url: string | null",
        "    channel_name: string | null",
        "    notify_on_new_feedback: boolean",
        "    notify_on_status_change: boolean",
        "    notify_on_new_comment: boolean",
        "    is_active: boolean",
        "    created_at: string",
        "    updated_at: string",
        "  }",
        "Step 3: Save the file",
        "Step 4: Run: npm run build",
        "Step 5: Run: git add -A && git commit -m 'feat: add Integration type'"
    ],
    "passes": false
  },
  {
    "id": 17,
      "category": "email",
      "description": "Install Resend and create email utility",
    "steps": [
        "Step 1: Run: npm install resend",
        "Step 2: Create folder lib/email",
        "Step 3: Create file lib/email/resend.ts",
        "Step 4: Add content:",
        "  import { Resend } from 'resend'",
        "  ",
        "  export const resend = new Resend(process.env.RESEND_API_KEY)",
        "  ",
        "  export async function sendEmail({",
        "    to,",
        "    subject,",
        "    html,",
        "  }: {",
        "    to: string",
        "    subject: string",
        "    html: string",
        "  }) {",
        "    try {",
        "      const { data, error } = await resend.emails.send({",
        "        from: process.env.EMAIL_FROM || 'FeedbackHub <noreply@feedbackhub.app>',",
        "        to,",
        "        subject,",
        "        html,",
        "      })",
        "      if (error) {",
        "        console.error('Email error:', error)",
        "        return { success: false, error }",
        "      }",
        "      return { success: true, data }",
        "    } catch (error) {",
        "      console.error('Email error:', error)",
        "      return { success: false, error }",
        "    }",
        "  }",
        "Step 5: Save the file",
        "Step 6: Run: npm run build",
        "Step 7: Run: git add -A && git commit -m 'feat: add Resend email utility'"
    ],
    "passes": false
  },
  {
    "id": 18,
      "category": "email",
      "description": "Create email templates file",
    "steps": [
        "Step 1: Create file lib/email/templates.ts",
        "Step 2: Add statusChangeEmail template:",
        "  export function statusChangeEmail(data: { postTitle: string; oldStatus: string; newStatus: string; boardName: string; orgName: string; postUrl: string }) {",
        "    return `<div style=\"font-family: sans-serif; max-width: 600px; margin: 0 auto;\">",
        "      <h2>Status Update</h2>",
        "      <p>Your feedback <strong>${data.postTitle}</strong> has been updated.</p>",
        "      <p>Status: <strong>${data.oldStatus}</strong> → <strong>${data.newStatus}</strong></p>",
        "      <p>Board: ${data.boardName} • ${data.orgName}</p>",
        "      <a href=\"${data.postUrl}\" style=\"display: inline-block; padding: 12px 24px; background: #000; color: #fff; text-decoration: none; border-radius: 6px;\">View Post</a>",
        "    </div>`",
        "  }",
        "Step 3: Add newCommentEmail template:",
        "  export function newCommentEmail(data: { postTitle: string; commenterName: string; commentContent: string; postUrl: string }) {",
        "    return `<div style=\"font-family: sans-serif; max-width: 600px; margin: 0 auto;\">",
        "      <h2>New Comment</h2>",
        "      <p><strong>${data.commenterName}</strong> commented on <strong>${data.postTitle}</strong></p>",
        "      <blockquote style=\"border-left: 3px solid #ccc; padding-left: 12px; margin: 16px 0;\">${data.commentContent}</blockquote>",
        "      <a href=\"${data.postUrl}\" style=\"display: inline-block; padding: 12px 24px; background: #000; color: #fff; text-decoration: none; border-radius: 6px;\">View Comment</a>",
        "    </div>`",
        "  }",
        "Step 4: Add newFeedbackEmail template:",
        "  export function newFeedbackEmail(data: { postTitle: string; postContent: string; authorName: string; boardName: string; postUrl: string }) {",
        "    return `<div style=\"font-family: sans-serif; max-width: 600px; margin: 0 auto;\">",
        "      <h2>New Feedback Received</h2>",
        "      <p><strong>${data.authorName || 'Someone'}</strong> submitted feedback to <strong>${data.boardName}</strong></p>",
        "      <h3>${data.postTitle}</h3>",
        "      <p>${data.postContent || 'No description'}</p>",
        "      <a href=\"${data.postUrl}\" style=\"display: inline-block; padding: 12px 24px; background: #000; color: #fff; text-decoration: none; border-radius: 6px;\">Review Feedback</a>",
        "    </div>`",
        "  }",
        "Step 5: Add teamInviteEmail template:",
        "  export function teamInviteEmail(data: { orgName: string; inviterName: string; role: string; inviteUrl: string }) {",
        "    return `<div style=\"font-family: sans-serif; max-width: 600px; margin: 0 auto;\">",
        "      <h2>You're Invited!</h2>",
        "      <p><strong>${data.inviterName}</strong> invited you to join <strong>${data.orgName}</strong> as a ${data.role}.</p>",
        "      <a href=\"${data.inviteUrl}\" style=\"display: inline-block; padding: 12px 24px; background: #000; color: #fff; text-decoration: none; border-radius: 6px;\">Accept Invitation</a>",
        "      <p style=\"margin-top: 16px; color: #666; font-size: 14px;\">This invitation expires in 7 days.</p>",
        "    </div>`",
        "  }",
        "Step 6: Add passwordResetEmail template:",
        "  export function passwordResetEmail(data: { resetUrl: string }) {",
        "    return `<div style=\"font-family: sans-serif; max-width: 600px; margin: 0 auto;\">",
        "      <h2>Reset Your Password</h2>",
        "      <p>Click the button below to reset your password.</p>",
        "      <a href=\"${data.resetUrl}\" style=\"display: inline-block; padding: 12px 24px; background: #000; color: #fff; text-decoration: none; border-radius: 6px;\">Reset Password</a>",
        "      <p style=\"margin-top: 16px; color: #666; font-size: 14px;\">If you didn't request this, ignore this email.</p>",
        "    </div>`",
        "  }",
        "Step 7: Save the file",
        "Step 8: Run: npm run build",
        "Step 9: Run: git add -A && git commit -m 'feat: add email templates'"
    ],
    "passes": false
  },
  {
    "id": 19,
      "category": "api",
      "description": "Create tags API - GET and POST",
    "steps": [
        "Step 1: Create folder app/api/tags",
        "Step 2: Create file app/api/tags/route.ts",
        "Step 3: Add imports:",
        "  import { createClient } from '@/lib/supabase/server'",
        "  import { NextResponse } from 'next/server'",
        "Step 4: Add GET function:",
        "  export async function GET(request: Request) {",
        "    const { searchParams } = new URL(request.url)",
        "    const orgId = searchParams.get('org_id')",
        "    if (!orgId) return NextResponse.json({ error: 'org_id required' }, { status: 400 })",
        "    const supabase = await createClient()",
        "    const { data, error } = await supabase.from('tags').select('*').eq('org_id', orgId).order('name')",
        "    if (error) return NextResponse.json({ error: error.message }, { status: 500 })",
        "    return NextResponse.json({ tags: data })",
        "  }",
        "Step 5: Add POST function:",
        "  export async function POST(request: Request) {",
        "    const body = await request.json()",
        "    const { org_id, name, color } = body",
        "    if (!org_id || !name) return NextResponse.json({ error: 'org_id and name required' }, { status: 400 })",
        "    const supabase = await createClient()",
        "    const { data, error } = await supabase.from('tags').insert({ org_id, name, color: color || '#6B7280' }).select().single()",
        "    if (error) return NextResponse.json({ error: error.message }, { status: 500 })",
        "    return NextResponse.json({ tag: data })",
        "  }",
        "Step 6: Save the file",
        "Step 7: Run: npm run build",
        "Step 8: Run: git add -A && git commit -m 'feat: add tags API GET and POST'"
    ],
    "passes": false
  },
  {
    "id": 20,
      "category": "api",
      "description": "Create tags DELETE API",
    "steps": [
        "Step 1: Create folder app/api/tags/[id]",
        "Step 2: Create file app/api/tags/[id]/route.ts",
        "Step 3: Add imports:",
        "  import { createClient } from '@/lib/supabase/server'",
        "  import { NextResponse } from 'next/server'",
        "Step 4: Add DELETE function:",
        "  export async function DELETE(request: Request, { params }: { params: { id: string } }) {",
        "    const supabase = await createClient()",
        "    const { error } = await supabase.from('tags').delete().eq('id', params.id)",
        "    if (error) return NextResponse.json({ error: error.message }, { status: 500 })",
        "    return NextResponse.json({ success: true })",
        "  }",
        "Step 5: Save the file",
        "Step 6: Run: npm run build",
        "Step 7: Run: git add -A && git commit -m 'feat: add tags DELETE API'"
    ],
    "passes": false
  },
  {
    "id": 21,
      "category": "api",
      "description": "Create post tags API",
    "steps": [
        "Step 1: Create folder app/api/posts/[id]/tags",
        "Step 2: Create file app/api/posts/[id]/tags/route.ts",
        "Step 3: Add imports",
        "Step 4: Add GET function to list tags on a post:",
        "  export async function GET(request: Request, { params }: { params: { id: string } }) {",
        "    const supabase = await createClient()",
        "    const { data, error } = await supabase.from('post_tags').select('tag_id, tags(*)').eq('post_id', params.id)",
        "    if (error) return NextResponse.json({ error: error.message }, { status: 500 })",
        "    return NextResponse.json({ tags: data?.map(pt => pt.tags) || [] })",
        "  }",
        "Step 5: Add POST function to add tag to post:",
        "  export async function POST(request: Request, { params }: { params: { id: string } }) {",
        "    const body = await request.json()",
        "    const { tag_id } = body",
        "    const supabase = await createClient()",
        "    const { error } = await supabase.from('post_tags').insert({ post_id: params.id, tag_id })",
        "    if (error) return NextResponse.json({ error: error.message }, { status: 500 })",
        "    return NextResponse.json({ success: true })",
        "  }",
        "Step 6: Add DELETE function to remove tag from post:",
        "  export async function DELETE(request: Request, { params }: { params: { id: string } }) {",
        "    const { searchParams } = new URL(request.url)",
        "    const tagId = searchParams.get('tag_id')",
        "    const supabase = await createClient()",
        "    const { error } = await supabase.from('post_tags').delete().eq('post_id', params.id).eq('tag_id', tagId)",
        "    if (error) return NextResponse.json({ error: error.message }, { status: 500 })",
        "    return NextResponse.json({ success: true })",
        "  }",
        "Step 7: Save the file",
        "Step 8: Run: npm run build",
        "Step 9: Run: git add -A && git commit -m 'feat: add post tags API'"
    ],
    "passes": false
  },
  {
    "id": 22,
      "category": "api",
      "description": "Create invitations API",
    "steps": [
        "Step 1: Create folder app/api/invitations",
        "Step 2: Create file app/api/invitations/route.ts",
        "Step 3: Add imports",
        "Step 4: Add GET function to list pending invitations:",
        "  export async function GET(request: Request) {",
        "    const { searchParams } = new URL(request.url)",
        "    const orgId = searchParams.get('org_id')",
        "    const supabase = await createClient()",
        "    const { data, error } = await supabase.from('invitations').select('*').eq('org_id', orgId).is('accepted_at', null).order('created_at', { ascending: false })",
        "    if (error) return NextResponse.json({ error: error.message }, { status: 500 })",
        "    return NextResponse.json({ invitations: data })",
        "  }",
        "Step 5: Add POST function to create invitation:",
        "  export async function POST(request: Request) {",
        "    const body = await request.json()",
        "    const { org_id, email, role, invited_by } = body",
        "    const token = crypto.randomUUID()",
        "    const expiresAt = new Date(); expiresAt.setDate(expiresAt.getDate() + 7)",
        "    const supabase = await createClient()",
        "    const { data, error } = await supabase.from('invitations').insert({ org_id, email, role: role || 'member', invited_by, token, expires_at: expiresAt.toISOString() }).select().single()",
        "    if (error) return NextResponse.json({ error: error.message }, { status: 500 })",
        "    return NextResponse.json({ invitation: data })",
        "  }",
        "Step 6: Save the file",
        "Step 7: Run: npm run build",
        "Step 8: Run: git add -A && git commit -m 'feat: add invitations API'"
    ],
    "passes": false
  },
  {
    "id": 23,
      "category": "api",
      "description": "Create invitation accept API",
    "steps": [
        "Step 1: Create folder app/api/invitations/accept",
        "Step 2: Create file app/api/invitations/accept/route.ts",
        "Step 3: Add imports",
        "Step 4: Add POST function:",
        "  export async function POST(request: Request) {",
        "    const body = await request.json()",
        "    const { token, user_id } = body",
        "    const supabase = await createClient()",
        "    // Find invitation",
        "    const { data: invitation, error: findError } = await supabase.from('invitations').select('*').eq('token', token).is('accepted_at', null).single()",
        "    if (findError || !invitation) return NextResponse.json({ error: 'Invalid or expired invitation' }, { status: 400 })",
        "    // Check expiry",
        "    if (new Date(invitation.expires_at) < new Date()) return NextResponse.json({ error: 'Invitation expired' }, { status: 400 })",
        "    // Add user to org",
        "    const { error: memberError } = await supabase.from('org_members').insert({ org_id: invitation.org_id, user_id, role: invitation.role })",
        "    if (memberError) return NextResponse.json({ error: memberError.message }, { status: 500 })",
        "    // Mark accepted",
        "    await supabase.from('invitations').update({ accepted_at: new Date().toISOString() }).eq('id', invitation.id)",
        "    return NextResponse.json({ success: true, org_id: invitation.org_id })",
        "  }",
        "Step 5: Save the file",
        "Step 6: Run: npm run build",
        "Step 7: Run: git add -A && git commit -m 'feat: add invitation accept API'"
    ],
    "passes": false
  },
  {
    "id": 24,
      "category": "api",
      "description": "Create team members API",
      "steps": [
        "Step 1: Create folder app/api/team",
        "Step 2: Create file app/api/team/route.ts",
        "Step 3: Add imports",
        "Step 4: Add GET function:",
        "  export async function GET(request: Request) {",
        "    const { searchParams } = new URL(request.url)",
        "    const orgId = searchParams.get('org_id')",
        "    const supabase = await createClient()",
        "    const { data, error } = await supabase.from('org_members').select('*').eq('org_id', orgId).order('created_at')",
        "    if (error) return NextResponse.json({ error: error.message }, { status: 500 })",
        "    return NextResponse.json({ members: data })",
        "  }",
        "Step 5: Save the file",
        "Step 6: Run: npm run build",
        "Step 7: Run: git add -A && git commit -m 'feat: add team members API'"
      ],
      "passes": true
    },
    {
      "id": 25,
      "category": "api",
      "description": "Create team member update and remove API",
      "steps": [
        "Step 1: Create folder app/api/team/[userId]",
        "Step 2: Create file app/api/team/[userId]/route.ts",
        "Step 3: Add imports",
        "Step 4: Add PATCH function for role update:",
        "  export async function PATCH(request: Request, { params }: { params: { userId: string } }) {",
        "    const body = await request.json()",
        "    const { org_id, role } = body",
        "    const supabase = await createClient()",
        "    const { error } = await supabase.from('org_members').update({ role }).eq('org_id', org_id).eq('user_id', params.userId)",
        "    if (error) return NextResponse.json({ error: error.message }, { status: 500 })",
        "    return NextResponse.json({ success: true })",
        "  }",
        "Step 5: Add DELETE function:",
        "  export async function DELETE(request: Request, { params }: { params: { userId: string } }) {",
        "    const { searchParams } = new URL(request.url)",
        "    const orgId = searchParams.get('org_id')",
        "    const supabase = await createClient()",
        "    const { error } = await supabase.from('org_members').delete().eq('org_id', orgId).eq('user_id', params.userId)",
        "    if (error) return NextResponse.json({ error: error.message }, { status: 500 })",
        "    return NextResponse.json({ success: true })",
        "  }",
        "Step 6: Save the file",
        "Step 7: Run: npm run build",
        "Step 8: Run: git add -A && git commit -m 'feat: add team member update and remove API'"
      ],
      "passes": false
    },
    {
      "id": 26,
      "category": "api",
      "description": "Create webhooks API",
      "steps": [
        "Step 1: Create folder app/api/webhooks-settings",
        "Step 2: Create file app/api/webhooks-settings/route.ts",
        "Step 3: Add imports",
        "Step 4: Add GET function:",
        "  export async function GET(request: Request) {",
        "    const { searchParams } = new URL(request.url)",
        "    const orgId = searchParams.get('org_id')",
        "    const supabase = await createClient()",
        "    const { data, error } = await supabase.from('webhooks').select('*').eq('org_id', orgId).order('created_at', { ascending: false })",
        "    if (error) return NextResponse.json({ error: error.message }, { status: 500 })",
        "    return NextResponse.json({ webhooks: data })",
        "  }",
        "Step 5: Add POST function:",
        "  export async function POST(request: Request) {",
        "    const body = await request.json()",
        "    const { org_id, name, url, events, secret } = body",
        "    const supabase = await createClient()",
        "    const { data, error } = await supabase.from('webhooks').insert({ org_id, name, url, events: events || [], secret }).select().single()",
        "    if (error) return NextResponse.json({ error: error.message }, { status: 500 })",
        "    return NextResponse.json({ webhook: data })",
        "  }",
        "Step 6: Save the file",
        "Step 7: Run: npm run build",
        "Step 8: Run: git add -A && git commit -m 'feat: add webhooks settings API'"
      ],
      "passes": false
    },
    {
      "id": 27,
      "category": "api",
      "description": "Create webhooks delete API",
      "steps": [
        "Step 1: Create folder app/api/webhooks-settings/[id]",
        "Step 2: Create file app/api/webhooks-settings/[id]/route.ts",
        "Step 3: Add imports",
        "Step 4: Add DELETE function:",
        "  export async function DELETE(request: Request, { params }: { params: { id: string } }) {",
        "    const supabase = await createClient()",
        "    const { error } = await supabase.from('webhooks').delete().eq('id', params.id)",
        "    if (error) return NextResponse.json({ error: error.message }, { status: 500 })",
        "    return NextResponse.json({ success: true })",
        "  }",
        "Step 5: Add PATCH function for toggle:",
        "  export async function PATCH(request: Request, { params }: { params: { id: string } }) {",
        "    const body = await request.json()",
        "    const supabase = await createClient()",
        "    const { error } = await supabase.from('webhooks').update(body).eq('id', params.id)",
        "    if (error) return NextResponse.json({ error: error.message }, { status: 500 })",
        "    return NextResponse.json({ success: true })",
        "  }",
        "Step 6: Save the file",
        "Step 7: Run: npm run build",
        "Step 8: Run: git add -A && git commit -m 'feat: add webhooks delete API'"
      ],
      "passes": false
    },
    {
      "id": 28,
      "category": "lib",
      "description": "Create webhook firing utility",
      "steps": [
        "Step 1: Create folder lib/webhooks",
        "Step 2: Create file lib/webhooks/fire.ts",
        "Step 3: Add content:",
        "  import { createClient } from '@/lib/supabase/server'",
        "  import crypto from 'crypto'",
        "  ",
        "  export type WebhookEvent = 'post.created' | 'post.status_changed' | 'post.voted' | 'comment.created' | 'changelog.published'",
        "  ",
        "  export async function fireWebhooks({ orgId, event, payload }: { orgId: string; event: WebhookEvent; payload: any }) {",
        "    const supabase = await createClient()",
        "    const { data: webhooks } = await supabase.from('webhooks').select('*').eq('org_id', orgId).eq('is_active', true).contains('events', [event])",
        "    if (!webhooks || webhooks.length === 0) return",
        "    const body = JSON.stringify({ event, payload, timestamp: new Date().toISOString() })",
        "    await Promise.all(webhooks.map(async (webhook) => {",
        "      const headers: Record<string, string> = { 'Content-Type': 'application/json' }",
        "      if (webhook.secret) {",
        "        headers['X-Webhook-Signature'] = crypto.createHmac('sha256', webhook.secret).update(body).digest('hex')",
        "      }",
        "      try { await fetch(webhook.url, { method: 'POST', headers, body }) } catch (err) { console.error('Webhook error:', err) }",
        "    }))",
        "  }",
        "Step 4: Save the file",
        "Step 5: Run: npm run build",
        "Step 6: Run: git add -A && git commit -m 'feat: add webhook firing utility'"
      ],
      "passes": false
    },
    {
      "id": 29,
      "category": "api",
      "description": "Create API keys API",
      "steps": [
        "Step 1: Create folder app/api/api-keys",
        "Step 2: Create file app/api/api-keys/route.ts",
        "Step 3: Add imports including crypto",
        "Step 4: Add GET function:",
        "  export async function GET(request: Request) {",
        "    const { searchParams } = new URL(request.url)",
        "    const orgId = searchParams.get('org_id')",
        "    const supabase = await createClient()",
        "    const { data, error } = await supabase.from('api_keys').select('id, org_id, name, key_prefix, last_used_at, expires_at, created_at').eq('org_id', orgId).order('created_at', { ascending: false })",
        "    if (error) return NextResponse.json({ error: error.message }, { status: 500 })",
        "    return NextResponse.json({ apiKeys: data })",
        "  }",
        "Step 5: Add POST function:",
        "  export async function POST(request: Request) {",
        "    const body = await request.json()",
        "    const { org_id, name } = body",
        "    const rawKey = 'fh_' + crypto.randomBytes(32).toString('hex')",
        "    const keyPrefix = rawKey.substring(0, 10) + '...'",
        "    const keyHash = crypto.createHash('sha256').update(rawKey).digest('hex')",
        "    const supabase = await createClient()",
        "    const { data, error } = await supabase.from('api_keys').insert({ org_id, name, key_hash: keyHash, key_prefix: keyPrefix }).select().single()",
        "    if (error) return NextResponse.json({ error: error.message }, { status: 500 })",
        "    return NextResponse.json({ apiKey: data, rawKey })",
        "  }",
        "Step 6: Save the file",
        "Step 7: Run: npm run build",
        "Step 8: Run: git add -A && git commit -m 'feat: add API keys API'"
      ],
      "passes": false
    },
    {
      "id": 30,
      "category": "api",
      "description": "Create API key delete API",
      "steps": [
        "Step 1: Create folder app/api/api-keys/[id]",
        "Step 2: Create file app/api/api-keys/[id]/route.ts",
        "Step 3: Add imports",
        "Step 4: Add DELETE function:",
        "  export async function DELETE(request: Request, { params }: { params: { id: string } }) {",
        "    const supabase = await createClient()",
        "    const { error } = await supabase.from('api_keys').delete().eq('id', params.id)",
        "    if (error) return NextResponse.json({ error: error.message }, { status: 500 })",
        "    return NextResponse.json({ success: true })",
        "  }",
        "Step 5: Save the file",
        "Step 6: Run: npm run build",
        "Step 7: Run: git add -A && git commit -m 'feat: add API key delete API'"
      ],
      "passes": false
    },
    {
      "id": 31,
      "category": "api",
      "description": "Create integrations API",
      "steps": [
        "Step 1: Create folder app/api/integrations",
        "Step 2: Create file app/api/integrations/route.ts",
        "Step 3: Add imports",
        "Step 4: Add GET function:",
        "  export async function GET(request: Request) {",
        "    const { searchParams } = new URL(request.url)",
        "    const orgId = searchParams.get('org_id')",
        "    const supabase = await createClient()",
        "    const { data, error } = await supabase.from('integrations').select('*').eq('org_id', orgId)",
        "    if (error) return NextResponse.json({ error: error.message }, { status: 500 })",
        "    return NextResponse.json({ integrations: data })",
        "  }",
        "Step 5: Add POST function:",
        "  export async function POST(request: Request) {",
        "    const body = await request.json()",
        "    const { org_id, type, webhook_url, channel_name, notify_on_new_feedback, notify_on_status_change, notify_on_new_comment } = body",
        "    const supabase = await createClient()",
        "    const { data, error } = await supabase.from('integrations').upsert({ org_id, type, webhook_url, channel_name, notify_on_new_feedback: notify_on_new_feedback ?? true, notify_on_status_change: notify_on_status_change ?? true, notify_on_new_comment: notify_on_new_comment ?? false, is_active: true, updated_at: new Date().toISOString() }, { onConflict: 'org_id,type' }).select().single()",
        "    if (error) return NextResponse.json({ error: error.message }, { status: 500 })",
        "    return NextResponse.json({ integration: data })",
        "  }",
        "Step 6: Save the file",
        "Step 7: Run: npm run build",
        "Step 8: Run: git add -A && git commit -m 'feat: add integrations API'"
      ],
      "passes": false
    },
    {
      "id": 32,
      "category": "lib",
      "description": "Create Slack/Discord notification utility",
      "steps": [
        "Step 1: Create folder lib/integrations",
        "Step 2: Create file lib/integrations/notify.ts",
        "Step 3: Add content:",
        "  import { createClient } from '@/lib/supabase/server'",
        "  type NotificationType = 'new_feedback' | 'status_change' | 'new_comment'",
        "  interface NotificationPayload { title: string; description: string; url: string; color?: string }",
        "  ",
        "  export async function notifyIntegrations({ orgId, type, payload }: { orgId: string; type: NotificationType; payload: NotificationPayload }) {",
        "    const supabase = await createClient()",
        "    const { data: integrations } = await supabase.from('integrations').select('*').eq('org_id', orgId).eq('is_active', true)",
        "    if (!integrations) return",
        "    for (const i of integrations) {",
        "      if (type === 'new_feedback' && !i.notify_on_new_feedback) continue",
        "      if (type === 'status_change' && !i.notify_on_status_change) continue",
        "      if (type === 'new_comment' && !i.notify_on_new_comment) continue",
        "      if (i.type === 'slack') await sendSlack(i.webhook_url, payload)",
        "      if (i.type === 'discord') await sendDiscord(i.webhook_url, payload)",
        "    }",
        "  }",
        "  ",
        "  async function sendSlack(url: string, p: NotificationPayload) {",
        "    await fetch(url, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ blocks: [{ type: 'section', text: { type: 'mrkdwn', text: `*${p.title}*\\n${p.description}` } }, { type: 'actions', elements: [{ type: 'button', text: { type: 'plain_text', text: 'View' }, url: p.url }] }] }) })",
        "  }",
        "  ",
        "  async function sendDiscord(url: string, p: NotificationPayload) {",
        "    await fetch(url, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ embeds: [{ title: p.title, description: p.description, url: p.url, color: parseInt((p.color || '#000000').replace('#', ''), 16) }] }) })",
        "  }",
        "Step 4: Save the file",
        "Step 5: Run: npm run build",
        "Step 6: Run: git add -A && git commit -m 'feat: add Slack Discord notification utility'"
      ],
      "passes": false
    },
    {
      "id": 33,
      "category": "api",
      "description": "Create notification preferences API",
      "steps": [
        "Step 1: Create folder app/api/notification-preferences",
        "Step 2: Create file app/api/notification-preferences/route.ts",
        "Step 3: Add imports",
        "Step 4: Add GET function that returns defaults if not found",
        "Step 5: Add POST function to upsert preferences",
        "Step 6: Save the file",
        "Step 7: Run: npm run build",
        "Step 8: Run: git add -A && git commit -m 'feat: add notification preferences API'"
      ],
      "passes": false
    },
    {
      "id": 34,
      "category": "api",
      "description": "Create organization settings API",
      "steps": [
        "Step 1: Create folder app/api/organizations/[id]",
        "Step 2: Create file app/api/organizations/[id]/route.ts",
        "Step 3: Add GET function to fetch org",
        "Step 4: Add PATCH function to update org name, slug, description, logo_url, website",
        "Step 5: Save the file",
        "Step 6: Run: npm run build",
        "Step 7: Run: git add -A && git commit -m 'feat: add organization settings API'"
      ],
      "passes": false
    },
    {
      "id": 35,
      "category": "api",
      "description": "Update boards API with archive and delete",
      "steps": [
        "Step 1: Open file app/api/boards/[id]/route.ts",
        "Step 2: Update PATCH to handle is_archived field",
        "Step 3: Add DELETE function if not exists",
        "Step 4: Save the file",
        "Step 5: Run: npm run build",
        "Step 6: Run: git add -A && git commit -m 'feat: add board archive and delete'"
      ],
      "passes": false
    },
    {
      "id": 36,
      "category": "api",
      "description": "Create password reset API",
      "steps": [
        "Step 1: Create folder app/api/auth/reset-password",
        "Step 2: Create file app/api/auth/reset-password/route.ts",
        "Step 3: Add POST function that calls supabase.auth.resetPasswordForEmail with redirectTo",
        "Step 4: Save the file",
        "Step 5: Run: npm run build",
        "Step 6: Run: git add -A && git commit -m 'feat: add password reset API'"
      ],
      "passes": false
    },
    {
      "id": 37,
      "category": "api",
      "description": "Create changelog subscribe API",
      "steps": [
        "Step 1: Create folder app/api/changelog/subscribe",
        "Step 2: Create file app/api/changelog/subscribe/route.ts",
        "Step 3: Add POST function to subscribe email to org changelog",
        "Step 4: Check for existing subscription",
        "Step 5: Add CORS headers for widget usage",
        "Step 6: Save the file",
        "Step 7: Run: npm run build",
        "Step 8: Run: git add -A && git commit -m 'feat: add changelog subscribe API'"
      ],
      "passes": false
    },
    {
      "id": 38,
      "category": "pages",
      "description": "Create forgot password page",
      "steps": [
        "Step 1: Create folder app/(auth)/forgot-password",
        "Step 2: Create file app/(auth)/forgot-password/page.tsx",
        "Step 3: Add 'use client'",
        "Step 4: Add imports: useState, Button, Input, Card, Label, Link",
        "Step 5: Create ForgotPasswordPage component with email form",
        "Step 6: On submit, POST to /api/auth/reset-password",
        "Step 7: Show success message after submission",
        "Step 8: Add link back to login",
        "Step 9: Save the file",
        "Step 10: Run: npm run build",
        "Step 11: Run: git add -A && git commit -m 'feat: add forgot password page'"
      ],
      "passes": false
    },
    {
      "id": 39,
      "category": "pages",
      "description": "Create reset password page",
      "steps": [
        "Step 1: Create folder app/auth/reset-password",
        "Step 2: Create file app/auth/reset-password/page.tsx",
        "Step 3: Add 'use client'",
        "Step 4: Add imports including createClient from client",
        "Step 5: Create form with new password and confirm password",
        "Step 6: On submit, call supabase.auth.updateUser({ password })",
        "Step 7: Redirect to login on success",
        "Step 8: Save the file",
        "Step 9: Run: npm run build",
        "Step 10: Run: git add -A && git commit -m 'feat: add reset password page'"
      ],
      "passes": false
    },
    {
      "id": 40,
      "category": "pages",
      "description": "Update login page with forgot password link",
      "steps": [
        "Step 1: Open file app/(auth)/login/page.tsx",
        "Step 2: Add Link import if not present",
        "Step 3: Add forgot password link below password field:",
        "  <Link href='/forgot-password' className='text-sm text-gray-600 hover:underline'>Forgot password?</Link>",
        "Step 4: Save the file",
        "Step 5: Run: npm run build",
        "Step 6: Run: git add -A && git commit -m 'feat: add forgot password link to login'"
      ],
      "passes": false
    },
    {
      "id": 41,
      "category": "pages",
      "description": "Create organization settings page",
      "steps": [
        "Step 1: Create folder app/(dashboard)/settings/organization",
        "Step 2: Create file app/(dashboard)/settings/organization/page.tsx",
        "Step 3: Fetch org data from supabase",
        "Step 4: Import and use OrgSettingsForm component (will create next)",
        "Step 5: Save the file",
        "Step 6: Run: npm run build",
        "Step 7: Run: git add -A && git commit -m 'feat: add org settings page'"
      ],
      "passes": false
    },
    {
      "id": 42,
      "category": "components",
      "description": "Create OrgSettingsForm component",
      "steps": [
        "Step 1: Create folder components/settings",
        "Step 2: Create file components/settings/org-settings-form.tsx",
        "Step 3: Add 'use client'",
        "Step 4: Add form with fields: name, slug, description, website",
        "Step 5: On submit, PATCH to /api/organizations/[id]",
        "Step 6: Show success toast",
        "Step 7: Save the file",
        "Step 8: Run: npm run build",
        "Step 9: Run: git add -A && git commit -m 'feat: add OrgSettingsForm component'"
      ],
      "passes": false
    },
    {
      "id": 43,
      "category": "pages",
      "description": "Create board settings page",
      "steps": [
        "Step 1: Create folder app/(dashboard)/boards/[id]/settings",
        "Step 2: Create file app/(dashboard)/boards/[id]/settings/page.tsx",
        "Step 3: Fetch board data",
        "Step 4: Import and use BoardSettingsForm component",
        "Step 5: Add danger zone with archive and delete buttons",
        "Step 6: Save the file",
        "Step 7: Run: npm run build",
        "Step 8: Run: git add -A && git commit -m 'feat: add board settings page'"
      ],
      "passes": false
    },
    {
      "id": 44,
      "category": "components",
      "description": "Create BoardSettingsForm component",
      "steps": [
        "Step 1: Create file components/settings/board-settings-form.tsx",
        "Step 2: Add 'use client'",
        "Step 3: Add form with fields: name, slug, description, is_public toggle",
        "Step 4: Add archive and delete functions with confirmations",
        "Step 5: Redirect to /boards after delete",
        "Step 6: Save the file",
        "Step 7: Run: npm run build",
        "Step 8: Run: git add -A && git commit -m 'feat: add BoardSettingsForm component'"
      ],
      "passes": false
    },
    {
      "id": 45,
      "category": "pages",
      "description": "Create team settings page",
      "steps": [
        "Step 1: Create folder app/(dashboard)/settings/team",
        "Step 2: Create file app/(dashboard)/settings/team/page.tsx",
        "Step 3: Fetch team members and pending invitations",
        "Step 4: Import InviteMemberForm and TeamMembersList components",
        "Step 5: Save the file",
        "Step 6: Run: npm run build",
        "Step 7: Run: git add -A && git commit -m 'feat: add team settings page'"
      ],
      "passes": false
    },
    {
      "id": 46,
      "category": "components",
      "description": "Create InviteMemberForm component",
      "steps": [
        "Step 1: Create file components/settings/invite-member-form.tsx",
        "Step 2: Add 'use client'",
        "Step 3: Add form with email input and role select (admin/member)",
        "Step 4: On submit, POST to /api/invitations",
        "Step 5: TODO: trigger email send",
        "Step 6: Save the file",
        "Step 7: Run: npm run build",
        "Step 8: Run: git add -A && git commit -m 'feat: add InviteMemberForm component'"
      ],
      "passes": false
    },
    {
      "id": 47,
      "category": "components",
      "description": "Create TeamMembersList component",
      "steps": [
        "Step 1: Create file components/settings/team-members-list.tsx",
        "Step 2: Add 'use client'",
        "Step 3: List members with email, role badge",
        "Step 4: Role change dropdown for non-owners",
        "Step 5: Remove button with confirmation",
        "Step 6: Save the file",
        "Step 7: Run: npm run build",
        "Step 8: Run: git add -A && git commit -m 'feat: add TeamMembersList component'"
      ],
      "passes": false
    },
    {
      "id": 48,
      "category": "pages",
      "description": "Create invitation accept page",
      "steps": [
        "Step 1: Create folder app/invite/[token]",
        "Step 2: Create file app/invite/[token]/page.tsx",
        "Step 3: Add 'use client'",
        "Step 4: Check if user is logged in",
        "Step 5: If not logged in, show login/signup prompt",
        "Step 6: If logged in, show Accept button",
        "Step 7: On accept, POST to /api/invitations/accept",
        "Step 8: Redirect to dashboard on success",
        "Step 9: Save the file",
        "Step 10: Run: npm run build",
        "Step 11: Run: git add -A && git commit -m 'feat: add invitation accept page'"
      ],
      "passes": false
    },
    {
      "id": 49,
      "category": "pages",
      "description": "Create webhooks settings page",
      "steps": [
        "Step 1: Create folder app/(dashboard)/settings/webhooks",
        "Step 2: Create file app/(dashboard)/settings/webhooks/page.tsx",
        "Step 3: Fetch existing webhooks",
        "Step 4: Create WebhooksManager client component for CRUD",
        "Step 5: List available events",
        "Step 6: Save the file",
        "Step 7: Run: npm run build",
        "Step 8: Run: git add -A && git commit -m 'feat: add webhooks settings page'"
      ],
      "passes": false
    },
    {
      "id": 50,
      "category": "pages",
      "description": "Create API keys settings page",
      "steps": [
        "Step 1: Create folder app/(dashboard)/settings/api-keys",
        "Step 2: Create file app/(dashboard)/settings/api-keys/page.tsx",
        "Step 3: Fetch existing API keys (only show prefix)",
        "Step 4: Create ApiKeysManager client component",
        "Step 5: Show full key only on creation",
        "Step 6: Save the file",
        "Step 7: Run: npm run build",
        "Step 8: Run: git add -A && git commit -m 'feat: add API keys settings page'"
      ],
      "passes": false
    },
    {
      "id": 51,
      "category": "pages",
      "description": "Create integrations settings page",
      "steps": [
        "Step 1: Create folder app/(dashboard)/settings/integrations",
        "Step 2: Create file app/(dashboard)/settings/integrations/page.tsx",
        "Step 3: Fetch existing integrations",
        "Step 4: Create cards for Slack and Discord",
        "Step 5: Each card has webhook URL input and notification toggles",
        "Step 6: Save the file",
        "Step 7: Run: npm run build",
        "Step 8: Run: git add -A && git commit -m 'feat: add integrations settings page'"
      ],
      "passes": false
    },
    {
      "id": 52,
      "category": "pages",
      "description": "Update settings page with navigation",
      "steps": [
        "Step 1: Open file app/(dashboard)/settings/page.tsx",
        "Step 2: Create settings hub with links to all settings pages:",
        "  - Organization, Team, Tags, Webhooks, API Keys, Integrations",
        "Step 3: Or redirect to /settings/organization as default",
        "Step 4: Save the file",
        "Step 5: Run: npm run build",
        "Step 6: Run: git add -A && git commit -m 'feat: update settings page with navigation'"
      ],
      "passes": false
    },
    {
      "id": 53,
      "category": "components",
      "description": "Create search and filter components",
      "steps": [
        "Step 1: Create file components/boards/search-input.tsx with debounced search",
        "Step 2: Create file components/boards/status-filter.tsx with status dropdown",
        "Step 3: Create file components/boards/sort-select.tsx with sort options",
        "Step 4: Save all files",
        "Step 5: Run: npm run build",
        "Step 6: Run: git add -A && git commit -m 'feat: add search and filter components'"
      ],
      "passes": false
    },
    {
      "id": 54,
      "category": "pages",
      "description": "Add search and filters to board detail page",
      "steps": [
        "Step 1: Open file app/(dashboard)/boards/[id]/page.tsx",
        "Step 2: Import SearchInput, StatusFilter, SortSelect",
        "Step 3: Add filter bar above posts",
        "Step 4: Use URL searchParams for filter state",
        "Step 5: Filter posts based on params",
        "Step 6: Save the file",
        "Step 7: Run: npm run build",
        "Step 8: Run: git add -A && git commit -m 'feat: add search and filters to board page'"
      ],
      "passes": false
    },
    {
      "id": 55,
      "category": "components",
      "description": "Create tag components",
      "steps": [
        "Step 1: Create file components/tags/tag-badge.tsx - colored badge",
        "Step 2: Create file components/tags/tag-select.tsx - multi-select for posts",
        "Step 3: Create file components/tags/tag-manager.tsx - CRUD for settings",
        "Step 4: Save all files",
        "Step 5: Run: npm run build",
        "Step 6: Run: git add -A && git commit -m 'feat: add tag components'"
      ],
      "passes": false
    },
    {
      "id": 56,
      "category": "pages",
      "description": "Create tags settings page",
      "steps": [
        "Step 1: Create folder app/(dashboard)/settings/tags",
        "Step 2: Create file app/(dashboard)/settings/tags/page.tsx",
        "Step 3: Fetch existing tags",
        "Step 4: Use TagManager component for CRUD",
        "Step 5: Save the file",
        "Step 6: Run: npm run build",
        "Step 7: Run: git add -A && git commit -m 'feat: add tags settings page'"
      ],
      "passes": false
    },
    {
      "id": 57,
      "category": "pages",
      "description": "Create 404 not found page",
      "steps": [
        "Step 1: Create file app/not-found.tsx",
        "Step 2: Add centered 404 message with home button",
        "Step 3: Save the file",
        "Step 4: Run: npm run build",
        "Step 5: Run: git add -A && git commit -m 'feat: add 404 page'"
      ],
      "passes": false
    },
    {
      "id": 58,
      "category": "components",
      "description": "Create empty state component",
      "steps": [
        "Step 1: Create file components/ui/empty-state.tsx",
        "Step 2: Add EmptyState component with icon, title, description, action props",
        "Step 3: Use in boards list when no boards",
        "Step 4: Use in posts list when no posts",
        "Step 5: Use in changelog when no entries",
        "Step 6: Save the file",
        "Step 7: Run: npm run build",
        "Step 8: Run: git add -A && git commit -m 'feat: add empty state component'"
      ],
      "passes": false
    },
    {
      "id": 59,
      "category": "pages",
      "description": "Add changelog subscribe form to public changelog",
      "steps": [
        "Step 1: Open file app/[orgSlug]/changelog/page.tsx",
        "Step 2: Add subscribe section at top with email input and button",
        "Step 3: POST to /api/changelog/subscribe on submit",
        "Step 4: Show success message",
        "Step 5: Save the file",
        "Step 6: Run: npm run build",
        "Step 7: Run: git add -A && git commit -m 'feat: add changelog subscribe form'"
      ],
      "passes": false
    },
    {
      "id": 60,
    "category": "test",
      "description": "Final build test",
    "steps": [
      "Step 1: Run: npm run build",
        "Step 2: Check for TypeScript errors",
      "Step 3: Fix any errors found",
      "Step 4: Run: npm run build again",
        "Step 5: Verify build succeeds",
      "Step 6: Run: git add -A",
        "Step 7: Run: git commit -m 'feat: overnight batch complete - email, settings, team, search, tags, webhooks, api keys, integrations'"
    ],
    "passes": false
  }
]